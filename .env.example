# API base URL, including the /api prefix. Production is the default in code; override for a dev client.
EXPO_PUBLIC_API_BASE_URL=https://api.vegangrove.org/api
# EAS project id from `eas init`; required for push tokens.
EAS_PROJECT_ID=
